#include <stdio.h>
/*
 * main - Entry point
 * Description: display alphabets in lowercase
 * Return : Always 0 (Success)
 */
int main(void)
{
	char ch;
	for( ch = 'a'; ch <= 'z'; ch++)
	{putchar(ch\n) ;}
	return (0);
}
